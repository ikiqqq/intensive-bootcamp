function makeNegative(num) {
    // Code?
  return -Math.abs(num);
  
  }
  console.log(makeNegative(7));

  //best solution

//   function makeNegative(num) {
//     return num < 0 ? num : -num;
//   }